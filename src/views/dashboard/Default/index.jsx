import { memo } from 'react';
import MainCard from '../../../ui-component/cards/MainCard';

const Dashboard = () => {
  // const [isLoading, setLoading] = useState(true);

  return <MainCard>Đây là trang chủ</MainCard>;
};

export default memo(Dashboard);
